// Drill Mode questions — add questions here as they come in.
// Structure: { id, domain ('D1'–'D5'), question, options: [a,b,c,d], correct: 0-3, explanation }

export const drillQuestions = [
  {
    id: 1,
    domain: 'D1',
    question: 'A developer productivity platform uses an automated documentation agent. The agent must always extract metadata from source files before generating documentation, but in testing it sometimes skips the metadata extraction step and produces documentation from incomplete information. What is the correct tool_choice configuration to enforce the mandatory first step?',
    options: [
      "Set tool_choice to 'auto' and add a system prompt instruction: 'You must call extract_metadata before any other tool.'",
      "Set tool_choice to 'any' so the model must call a tool, relying on the tool description to guide it to extract_metadata first.",
      "Set tool_choice to forced selection with the specific tool name extract_metadata for the first turn, then switch to 'auto' for subsequent turns.",
      "Remove all other tools except extract_metadata from the agent's configuration so it has no choice.",
    ],
    correct: 2,
    explanation: "tool_choice forced to a specific tool name deterministically makes Claude call extract_metadata on the first turn — no probability involved. Switching to auto after lets the agent use all other tools normally. Option A (system prompt) is probabilistic — the model can still skip it, which is exactly the bug. Option B (any) forces a tool call but not which one. Option D breaks the agent: removing all other tools means it can't generate docs after extracting metadata.",
  },
  {
    id: 2,
    domain: 'D2',
    question: "A developer productivity platform has a tool called analyze_content with the description 'Analyses content from various sources.' Logs show the agent uses it for web scraping, document parsing, and code analysis indiscriminately, leading to poor results for each use case. What is the most effective fix?",
    options: [
      'Add detailed few-shot examples to the system prompt showing which types of content the tool handles well and which it does not.',
      'Improve the tool description to list all supported content types and their expected formats.',
      'Split analyze_content into purpose-specific tools: extract_web_results, parse_document, and analyze_code, each with a narrow remit.',
      'Implement a routing layer that inspects the input content type and dispatches to the appropriate internal processing function within analyze_content.',
    ],
    correct: 2,
    explanation: "Splitting into purpose-specific tools fixes both the selection problem and the results problem: Claude can now choose the right tool semantically, and each tool can be optimised for its specific task. Option B (better description) still leaves one tool doing three different things — Claude selects it but results are still mixed. Option D (internal routing) keeps the vague external interface — Claude still blindly calls analyze_content for everything and the routing layer has to guess. Option A (few-shot) helps selection marginally but doesn't fix the underlying one-tool-does-everything design.",
  },
  {
    id: 3,
    domain: 'D2',
    question: "A team's shared .mcp.json configures a PostgreSQL MCP server. A developer adds a personal staging database MCP server to their ~/.claude.json, so both database tools are available in their session. When they query 'check user count', the agent calls the production tool instead of staging. What is the best resolution?",
    options: [
      'Remove the production database from .mcp.json during testing so only the staging server is available.',
      'Rename the staging tool to a distinct name, put its environment in the description, and add a session rule that staging tools take precedence.',
      'Move the personal staging server into the shared .mcp.json beside production and let the agent pick between them from each server\'s connection string.',
      'Use tool_choice with forced selection of the staging database tool for all turns during testing.',
    ],
    correct: 1,
    explanation: "Tool descriptions are the primary selection signal. When two tools have similar names and vague descriptions, Claude cannot distinguish which environment to target. Renaming the staging tool (e.g. query_staging_db) and embedding the environment context in its description lets Claude select correctly based on user intent. A session rule adds explicit guidance for the testing context. Option A disrupts the whole team. Option C puts personal staging credentials into the shared team config — wrong scope. Option D forces all tool calls to staging with tool_choice, breaking selection for everything else in the session.",
  },
  {
    id: 4,
    domain: 'D2',
    question: "A developer asks Claude Code to modify a function in a large file. The Edit tool fails with a 'non-unique match' error because the target text appears in multiple locations within the file. What is the correct recovery approach?",
    options: [
      'Switch to Bash with sed to perform the replacement using line numbers instead of text matching.',
      'Use Read to load the full file contents, then use Write to output the complete modified file with the change applied.',
      'Expand the Edit search string to include more surrounding context until it becomes unique in the file.',
      'Split the file into smaller files so that each occurrence appears in only one file, then use Edit on each.',
    ],
    correct: 2,
    explanation: "The correct recovery sequence is: expand the old_string with more surrounding context first — this is usually enough to make the match unique. Only if repeated attempts still fail (or the surrounding context is structurally similar throughout the file) do you fall back to Read + Write the full file. Option B is the fallback, not the first step. Option A (Bash sed with line numbers) is fragile and an anti-pattern when dedicated tools exist. Option D (splitting the file) is not a recovery step — it changes the codebase structure to work around a tool limitation.",
  },
  {
    id: 5,
    domain: 'D2',
    question: 'In structured error metadata for MCP tool responses, what are the four standard categories for the errorCategory field?',
    options: [
      'client, server, network, and timeout.',
      'transient, validation, business, and permission.',
      'fatal, warning, recoverable, and informational.',
      'authentication, authorisation, rate-limit, and schema.',
    ],
    correct: 1,
    explanation: "The four standard errorCategory values are: transient (temporary, safe to retry — network blips, timeouts), validation (bad input — wrong format, missing required fields), business (rule or logic violation — quota exceeded, invalid state transition), and permission (access denied). These categories tell Claude how to react: transient → retry, validation → fix input and retry, business → escalate or inform user, permission → escalate. Option A mirrors HTTP error taxonomy. Option C describes severity levels, not categories. Option D lists specific sub-types, not the four top-level categories.",
  },
  {
    id: 6,
    domain: 'D2',
    question: "A developer is investigating a data inconsistency in the platform codebase. They need to find all files that reference a specific Snowflake table name 'fact_revenue_daily' and then check the directory structure for related migration files. Which tool sequence is correct?",
    options: [
      "Glob for '**/*fact_revenue_daily*' to find references, then Glob for '**/migrations/**' to find migration files.",
      'Read all SQL files in the project to search for the table name, then Read all migration files.',
      "Grep for 'fact_revenue_daily' to find all content references, then Glob for '**/migrations/*.sql' to find migration files by path pattern.",
      "Grep for 'fact_revenue_daily' for both steps — first to find references, then to find migration files containing the table.",
    ],
    correct: 2,
    explanation: "Grep searches inside file contents — correct for finding all files that reference 'fact_revenue_daily'. Glob matches file paths and directory patterns — correct for the second step because 'check the directory structure' is the signal: you want to see what migration files exist by path (**/migrations/*.sql), not search inside them. Key signal words: 'directory structure' → Glob; 'files that reference X' → Grep. If the question instead said 'find migration files that reference the table,' D (Grep both times) would be correct. Option A uses Glob first — misses files that internally reference the table but aren't named after it. Option B (Read everything) is inefficient.",
  },
  {
    id: 7,
    domain: 'D2',
    question: 'A data-analysis agent connects to an MCP server that exposes 40 database tables. Before almost every query, the agent makes several exploratory tool calls to discover which tables exist and what columns they hold, adding latency and token cost to each task. The server author wants to remove this discovery overhead. What is the most effective change?',
    options: [
      'Expose the table catalogue and column schemas as MCP resources for upfront visibility.',
      'Add a describe_schema tool and instruct the agent in its system prompt to call it before every query.',
      'Enlarge the agent\'s context window so it can retain the results of the discovery calls across turns.',
      'Reduce the server to the five most frequently queried tables so there is less to discover.',
    ],
    correct: 0,
    explanation: "MCP resources are structured data exposed by the server for upfront visibility — they load at session start without requiring any tool call. Exposing the schema catalog as a resource means Claude has full table/column knowledge before the first query, eliminating discovery overhead entirely. Option B (describe_schema tool) still requires a tool call — it consolidates but does not eliminate the cost; also the system prompt instruction is probabilistic. Option C (larger context window) lets cached discovery results persist longer but the discovery calls still happen. Option D reduces the product scope rather than fixing the architecture.",
  },
  {
    id: 8,
    domain: 'D3',
    question: "Project-level .claude/CLAUDE.md says 'use 4-space indentation matching the existing codebase.' A senior architect has 'use 2-space indentation' in their user-level ~/.claude/CLAUDE.md. In recent sessions the architect's code has come back in 2 spaces and broken the build. The team needs a guarantee that 4-space indentation is applied on every save. What should they do?",
    options: [
      "Add a PostToolUse hook that runs the team's formatter after every Write/Edit, so 4-space indentation is enforced regardless of what Claude generates.",
      "Leave the rule in project-level .claude/CLAUDE.md — the more specific scope wins on conflicts, so the project rule will override the architect's user-level preference.",
      "Ask the architect to delete their user-level ~/.claude/CLAUDE.md so there is no conflict to resolve.",
      "Move the 4-space rule into a CLAUDE.local.md at the project root so it is appended last and reads after the user-level file.",
    ],
    correct: 0,
    explanation: "Signal word: 'guarantee' → deterministic enforcement → hook. CLAUDE.md files are concatenated and all loaded together — there is no scope-wins override mechanism. Claude sees both the 4-space and 2-space instructions simultaneously and may follow either. A PostToolUse hook that runs the team formatter (e.g. prettier) after every Write/Edit enforces indentation deterministically regardless of what Claude generated. Option B is wrong on two levels: 'more specific scope wins' is false for CLAUDE.md (they concatenate, not override), and even if it were true CLAUDE.md instructions are still probabilistic. Option D (load order) fails for the same reason — order doesn't matter when Claude sees both instructions. Option C is invasive and not a scalable team policy.",
  },
  {
    id: 9,
    domain: 'D3',
    question: "Order the CLAUDE.md-family configuration sources from broadest scope (applies to many projects) to most specific scope (applies only to certain files). Anthropic's memory docs list these in load order from broadest to most specific, so the more specific files end up later in the context window.",
    options: [
      'Project CLAUDE.md → user ~/.claude/CLAUDE.md → directory CLAUDE.md → .claude/rules/',
      'User ~/.claude/CLAUDE.md → project .claude/CLAUDE.md → directory CLAUDE.md → .claude/rules/',
      '.claude/rules/ → directory CLAUDE.md → project .claude/CLAUDE.md → user ~/.claude/CLAUDE.md',
      'User ~/.claude/CLAUDE.md → .claude/rules/ → project .claude/CLAUDE.md → directory CLAUDE.md',
    ],
    correct: 1,
    explanation: "Broadest to most specific: (1) User ~/.claude/CLAUDE.md — applies across every project for that user; (2) Project .claude/CLAUDE.md — applies to the entire repo; (3) Directory CLAUDE.md — loaded only when Claude is working in that subdirectory; (4) .claude/rules/ — most specific, file-pattern-based rules that activate only for matching file types. Load order matters because more specific instructions appear later in the context window and can refine or override broader guidance. Option A incorrectly places user after project. Option C is the full reverse. Option D misplaces .claude/rules/ above project level.",
  },
  {
    id: 10,
    domain: 'D4',
    question: 'A developer describes a code transformation in prose. Claude Code interprets it differently each time, producing inconsistent results. What technique should the developer try first?',
    options: [
      'Rewrite the prose description with more precise language and technical terminology.',
      'Provide 2-3 concrete input/output examples showing the exact before and after transformation.',
      'Use the interview pattern to have Claude ask clarifying questions before implementing.',
      'Write a test suite first and iterate by sharing test failures.',
    ],
    correct: 1,
    explanation: "When prose instructions produce inconsistent results, the fix is concrete examples — not more prose. Examples show the exact transformation unambiguously; prose (however precise) is still open to interpretation. Option A is exactly what the developer already tried — rewriting prose with more precise language is still prose. Option C (interview pattern) addresses unclear requirements, not inconsistent execution. Option D (test suite) is valid but heavyweight — few-shot examples are faster and more directly fix the root cause. Core pattern: prose failing → add few-shot examples showing exact before/after.",
  },
  {
    id: 11,
    domain: 'D4',
    question: 'In a multi-step prompt chain, what should happen between each step to maintain pipeline reliability?',
    options: [
      "Each step's output should be passed directly to the next step without interruption to maintain conversational flow.",
      "Validate between steps, checking each step's output meets the expected format and content before the next runs.",
      'The entire chain should run in a single API call to minimise latency and cost.',
      'A human reviewer should approve each intermediate output before the chain continues.',
    ],
    correct: 1,
    explanation: "Inter-step validation catches errors early, preventing cascading failures. Each step should produce focused output that is validated before the next step consumes it. This is a core principle of reliable prompt chaining. Option A (pass directly) propagates errors silently — a bad step 2 output corrupts step 3 with no way to catch it. Option C (single API call) is not possible for multi-step chains and would balloon context. Option D (human reviewer every step) is the right answer only for high-stakes workflows explicitly requiring human-in-the-loop — not the default reliability mechanism.",
  },
  {
    id: 12,
    domain: 'D4',
    question: 'Your extraction pipeline retries failed documents, but the retry simply resends the original document with the same prompt. Success rates on retries are only marginally better than the first attempt. Developers have identified that most failures are format mismatches where the model places values in the wrong fields. How should you improve the retry mechanism?',
    options: [
      'Increase the number of retries from 1 to 3, since extraction is non-deterministic and more attempts improve odds.',
      'Switch to a different model for retries so a fresh perspective catches the errors.',
      'Skip retries and route all failures directly to human review to avoid wasting API costs.',
      'Send the original document, the failed extraction, and the validation error naming the misplaced fields.',
    ],
    correct: 3,
    explanation: "Resending the same prompt gives Claude zero new information — it makes the same mistake again. The validation-retry loop pattern fixes this: send back the original document, what Claude produced, and the specific validation error (which fields were misplaced). Now Claude knows exactly what to correct and can target those specific fields. Option A (more retries, same prompt) multiplies the same failure — non-determinism helps slightly but the model is consistently making the same structural mistake. Option B (different model) still provides no information about what failed. Option C (human review immediately) skips auto-correction entirely; human review is the last resort after targeted retries are exhausted.",
  },
  {
    id: 13,
    domain: 'D4',
    question: "A moderation classification tool includes a detected_patterns array where the model lists specific patterns it identified (e.g. 'repeated slur targeting ethnicity'). Validation checks whether the detected patterns match the assigned category, and on mismatch retries with feedback like 'You classified this as spam but detected patterns of hate speech targeting ethnicity, please re-evaluate.' What is the primary benefit of the detected_patterns field in this workflow?",
    options: [
      "It lets the system auto-correct the category by overriding the model's classification with a rule-based pattern match.",
      'It provides an auditable evidence trail showing which specific content features drove each moderation decision.',
      'It lets validation detect reasoning inconsistencies and feed back targeted errors the model uses to self-correct.',
      'It increases classification accuracy by forcing the model to identify specific patterns before assigning a category.',
    ],
    correct: 2,
    explanation: "The detected_patterns field exposes the model's reasoning, which enables the validation-retry loop described in the question: validation compares patterns to the assigned category, detects the mismatch (patterns say hate speech, category says spam), and sends targeted feedback so the model can self-correct. Without this field, validation only sees the final category and cannot identify the inconsistency. Option B (audit trail) is a real benefit of the field but is a general benefit — the question asks for the primary benefit in this specific workflow (the retry loop). Option A misreads the workflow — the system doesn't override the model, it feeds back specific errors for re-evaluation. Option D (forces chain-of-thought) is a secondary effect.",
  },
  {
    id: 14,
    domain: 'D4',
    question: "The moderation system's prompt instructs Claude to 'be conservative when moderating and err on the side of caution.' Reviewers find that innocuous posts about cooking with knives, news articles about violence, and fictional war stories are all being flagged as policy violations. What is the root cause and fix?",
    options: [
      'The model is too sensitive — lower the temperature to reduce over-flagging.',
      "Replace 'be conservative' with explicit categorical criteria defining each violation category with concrete examples.",
      'Add an allowlist of safe topics (cooking, news, fiction) that should never be flagged.',
      "Add a second moderation pass that re-reads each flagged post under the same 'be conservative' guidance and strips out the false positives before any action.",
    ],
    correct: 1,
    explanation: "'Be conservative' is a vague rule — the model interprets proximity to violence or weapons as a violation, which is technically consistent with the instruction. The fix is replacing vague guidance with measurable, explicit criteria: define each violation category with concrete examples of what does and does not trigger it. Option A (lower temperature) makes output more deterministic but does not change what gets classified as a violation — temperature controls randomness, not sensitivity thresholds. Option C (allowlist) patches specific known topics but leaves the vague underlying rule intact; new edge cases will keep appearing. Option D (second pass with same prompt) produces the same over-flagging — same instruction = same interpretation.",
  },
  {
    id: 15,
    domain: 'D4',
    question: 'Your Claude Code agent generates unit tests in the CI pipeline. When given detailed instructions alone, it produces tests with inconsistent assertion styles: sometimes using expect().toBe(), sometimes assert.equal(), and occasionally mixing both in the same file. Adding more detailed instructions about assertion style did not fix the problem. What should you do next?',
    options: [
      'Switch to a different model that better follows formatting instructions.',
      'Add a linter post-processing step to automatically convert all assertions to a single style.',
      'Add 2-4 few-shot examples showing complete test files with the desired assertion style and reasoning for why that style was chosen over alternatives.',
      'Add 2-4 few-shot examples demonstrating the desired assertion style with reasoning for each testing decision, covering edge cases like async functions and error handling.',
    ],
    correct: 3,
    trap: {
      index: 2,
      note: 'Complete test files as examples bloat the prompt unnecessarily. Examples should be focused and concise — not entire files.',
    },
    explanation: "Prose instructions failing → add few-shot examples. D is the correct form: focused examples covering diverse edge cases (async, error handling) without inflating the prompt. Option C also uses few-shot examples but uses complete test files, which bloat the context unnecessarily — examples should be concise and targeted. Option A (different model) doesn't fix a prompting problem. Option B (linter post-processing) is a workaround that doesn't help the model learn the style and could mask ongoing problems.",
  },
  {
    id: 16,
    domain: 'D4',
    question: 'The moderation team is designing few-shot examples to teach Claude how to handle ambiguous content — posts that could be either satirical commentary or genuine policy violations depending on context. What is the recommended approach for structuring these few-shot examples?',
    options: [
      'Provide examples of clear violations and clear non-violations only, avoiding ambiguous cases to prevent confusion.',
      'Include 2-4 ambiguous-case examples with reasoning tags on how context, tone, and intent separate satire from violations.',
      'Provide 15-20 examples covering every possible type of satire to ensure comprehensive coverage.',
      'Provide examples showing only the correct classification label without reasoning, so the model focuses on the output format.',
    ],
    correct: 1,
    explanation: "The examples must match the actual hard cases — ambiguous posts are the problem, so the examples should show ambiguous cases, not easy ones. 2-4 examples is the right count (enough diversity without bloating the prompt). Reasoning tags are essential: they show the model the decision process (how context, tone, and intent combine to reach a verdict) so it can apply the same reasoning to unseen cases. Option A (clear cases only) skips the actual problem — the model already handles obvious violations. Option C (15-20 examples) bloats the prompt and risks lost-in-the-middle degradation; exhaustive coverage is neither possible nor needed. Option D (labels only) teaches output format but not decision logic — the model can't generalise to new ambiguous cases.",
  },
  {
    id: 17,
    domain: 'D5',
    question: 'A docs team has Claude Code generate architecture guides by synthesising source code, inline comments, commit messages, and ADRs. After several edits, the guides no longer indicate which statements came from which source. A developer later questions whether a specific architectural constraint in a guide is still valid. What approach preserves source attribution?',
    options: [
      'Instruct Claude Code to add footnotes to the generated documentation citing the original source for each statement.',
      'Maintain structured provenance metadata that maps each claim to its source file, line number, and retrieval date.',
      'Keep all original source files unchanged in a reference directory so developers can manually trace any claim back to its origin.',
      'Version-control the documentation and use git blame to trace each line back to the commit that created it.',
    ],
    correct: 1,
    trap: {
      index: 0,
      note: 'Footnotes live inside the document — they get edited away over time. The question describes exactly this failure.',
    },
    explanation: "Structured provenance metadata lives outside the document and maps each claim to its source file, line number, and retrieval date. This survives editing cycles (metadata isn't in the doc body), is machine-readable, and the retrieval date answers the developer's question about validity — you can check if the source has changed since extraction. Option A (footnotes) is the trap: the question itself describes its failure mode — 'after several edits the guides no longer indicate sources' — footnotes are prose inside the document and get removed or detached through edits. Option C (reference directory) provides raw materials but no mapping — manual grep through all sources. Option D (git blame) points to the commit that wrote the guide, not the original ADR or source the constraint came from.",
  },
  {
    id: 18,
    domain: 'D5',
    question: 'A legal document extraction pipeline has been running for three months with calibrated confidence thresholds. Extractions above 90% confidence are auto-approved without human review, saving 60% of reviewer time. A new client submits contracts written in an unusual two-column legal format the system has not encountered before. The system reports 92% confidence on these extractions. What is the appropriate safeguard?',
    options: [
      "Maintain stratified random sampling of high-confidence extractions across document types to catch the new format's error rate.",
      'Trust the calibrated confidence threshold since it was validated against labelled data and the system reports 92% confidence, which exceeds the 90% auto-approval threshold.',
      'Raise the confidence threshold from 90% to 98% for all document types to account for novel formats.',
      'Add a document format classifier that flags unknown formats for mandatory human review before any auto-approval.',
    ],
    correct: 0,
    trap: {
      index: 1,
      note: 'The threshold was calibrated on known formats. A novel format is a distribution shift — 92% confidence here is not the same signal as 92% on familiar formats.',
    },
    explanation: "The D5 principle: segment evaluation by document type before changing thresholds. Stratified random sampling across document types is the appropriate safeguard — it keeps the pipeline running while actively measuring the new format's actual error rate. You don't assume the calibrated threshold holds for an unseen format; you measure it. Option B is the trap: 'threshold was validated' sounds solid, but calibration was on known formats. A two-column layout is a distribution shift — 92% confidence on a novel format is not the same signal as 92% on familiar ones. Option D (format classifier + mandatory human review) is a larger architectural intervention, not a safeguard — and it stops all auto-approval for unknown formats rather than monitoring and adjusting. Option C (raise threshold for all types) penalises all known formats to compensate for one new format class.",
  },
  {
    id: 19,
    domain: 'D5',
    question: "A docs team processes a 500,000-line codebase by having Claude Code read files and generate docs, instructing it to 'summarise each module after documenting it, then discard the detailed source from context.' After processing 20 modules, the docs for module 21 reference class names from module 3 that were renamed in module 12. What caused this failure?",
    options: [
      "Progressive summarisation lost the rename detail: when module 12's summary replaced its source code, the rename was condensed to 'various refactoring changes', so the agent retained module 3's original class names from its earlier summary.",
      "The agent's context window overflowed, causing it to hallucinate class names from earlier modules.",
      "Claude Code cached module 3's source code and served the stale version instead of the updated file on disk.",
      "The model confused similar class names because it processed too many modules in a single session.",
    ],
    correct: 0,
    explanation: "Progressive summarisation is lossy. The summary of module 12 compressed the rename into a generic phrase ('various refactoring changes'), while module 3's summary still referenced the original class names. When the agent documented module 21, it found conflicting names across summaries and defaulted to the earlier, more prominent reference. Critical cross-module changes like renames must be tracked in a persistent facts block outside summarised context — not left to survive compaction. Option B is wrong because the team explicitly manages context by summarising and discarding source, so the context window is not overflowing — the problem is the quality of information retained in summaries, not overflow.",
  },
  {
    id: 20,
    domain: 'D5',
    question: "An agent receives 200 rows of Snowflake financial data and places it mid-prompt, between system instructions and the user's follow-up question. Asked to identify the row with the highest margin, the agent picks the 45th row (margin 32%) over the 142nd row (margin 47%). What cognitive bias is affecting the agent?",
    options: [
      'Recency bias — the agent prioritises data appearing near the end of the context window.',
      'The lost-in-the-middle effect: the agent favours the start and end, missing the row buried in the middle.',
      'Token limit truncation — the result set exceeds the context window and rows beyond the 100th are silently dropped.',
      'Anchoring bias — the agent fixates on the first high-margin row it encounters and stops scanning the rest.',
    ],
    correct: 1,
    explanation: "The data is placed mid-prompt (between system instructions and the user's question) — exactly the position that gets under-weighted. Row 45 is near the start of the data block; row 142 is buried deep in the middle. The agent picks the more prominent early reference over the higher value in the least-attended region. This is the lost-in-the-middle effect: attention is highest at the start and end of context, systematically under-weighting the middle. Fix: move critical data to the start or end, or use explicit headers so the model knows what to scan for. Recency bias (A) would have caused the agent to pick row 142 (late in the data) — the opposite of what happened. Anchoring bias (D) is a human cognitive bias, not the named LLM positional phenomenon being tested. Truncation (C) would cause rows past a cutoff to not exist — but the agent is making a choice between two rows, not hitting missing data.",
  },
  {
    id: 21,
    domain: 'D5',
    question: "In a data extraction pipeline, what does 'confidence calibration' mean and why is it required before automating decisions based on confidence scores?",
    options: [
      'It means training the model to always output confidence scores above 90% so that most extractions can be automated.',
      'It means verifying that when the model reports X% confidence, approximately X% of those predictions are actually correct, validated against labelled data.',
      "It means adjusting the model's temperature parameter until confidence scores fall within an acceptable range.",
      'It means running the same extraction multiple times and averaging the confidence scores to reduce variance.',
    ],
    correct: 1,
    explanation: "Confidence calibration means the model's reported confidence scores match its real-world accuracy: if it says 90% confident, it should be correct ~90% of the time on a labelled test set. This validation is required before automation because without it, a confidence threshold is just a number with no known relationship to accuracy. A poorly calibrated model might say 92% confident while being wrong 40% of the time — auto-approving at that threshold would silently pass wrong extractions at scale. Option A (push scores high) produces overconfidence, not accuracy. Option C (temperature) controls sampling randomness, not the confidence-accuracy relationship. Option D (averaging runs) reduces variance but does not validate scores against ground truth.",
  },
  {
    id: 22,
    domain: 'D1',
    question: 'A coordinator spawns four Claude Code subagents to explore a large codebase overnight: one maps the data layer, one traces API routes, one audits tests, one catalogues dependencies. Three hours in, the machine restarts. On rerun, the coordinator starts every exploration again from zero. What design change lets a restarted run continue from where the crash left off?',
    options: [
      "Resume the coordinator's previous session so the conversation history is restored.",
      'Have each agent export structured state to a manifest the coordinator loads on resume.',
      'Run the four explorations sequentially in one session so all findings stay in a single context.',
      'Wrap each subagent in automatic retries so transient failures cannot end the run.',
    ],
    correct: 1,
    explanation: "Each subagent writes its completed findings to a fixed-path structured manifest as it goes. On restart, the coordinator reads the manifest to see what is already done and only spawns agents for the incomplete work — skipping data layer (done) and API routes (done), resuming tests (in-progress), starting dependencies (not started). Option A (resume session) restores conversation history but subagent tool results are stale after a machine restart — in-context results cannot be trusted post-crash, and the coordinator session may itself be incomplete. Option C (sequential) means any crash loses everything before the crash point — same problem, just serialised. Option D (retries) handles transient failures like network timeouts; a machine restart kills the process entirely and retries cannot help.",
  },
  {
    id: 23,
    domain: 'D1',
    question: 'A customer support agent handles account deletion requests. The company policy requires explicit human approval before any account is permanently deleted. Currently, the agent sometimes processes deletions autonomously when the customer is insistent. What approval mechanism should the architect implement?',
    options: [
      "Add a system prompt instruction: 'Always get manager approval before processing account deletions'.",
      'Implement a PreToolUse hook on the delete_account tool that pauses execution and routes the request to a human approval queue before proceeding.',
      'Remove the delete_account tool from the agent entirely and require customers to call a separate phone line.',
      'Implement a PostToolUse hook that reviews the deletion after it has been processed and reverts it if no approval was recorded.',
    ],
    correct: 1,
    explanation: "PreToolUse hook = deterministic enforcement: it intercepts every delete_account call before execution and routes to a human queue — no exceptions, even when the customer is insistent. Option A (system prompt) is probabilistic — the agent already ignores it when customers push hard. Option D (PostToolUse) runs after deletion — you cannot reliably undo a completed account deletion. Option C (remove the tool) is architecturally valid if humans must always approve, but the question asks for an approval mechanism within the existing workflow, and C's 'separate phone line' is a UX regression. Note: if the tool has no legitimate autonomous use case ever, removing it is the cleaner design — but the exam answer is B as a targeted enforcement fix.",
  },
  {
    id: 24,
    domain: 'D1',
    question: "A customer support agent must redact credit card numbers from its responses before they reach the customer. The current system prompt instructs the agent to replace card numbers with asterisks, but QA testing reveals that 6% of responses still contain unredacted card numbers. What guardrail approach should the architect use?",
    options: [
      'Rewrite the system prompt with more explicit redaction instructions and add few-shot examples of correct redaction.',
      'Add a PostToolUse hook that regex-redacts credit card numbers from tool results before the model sees them.',
      'Implement a PreToolUse hook that blocks any tool call containing a credit card number in its parameters.',
      'Remove all tools that might return credit card numbers from the agent\'s allowedTools.',
    ],
    correct: 1,
    trap: {
      index: 2,
      note: 'PreToolUse inspects tool parameters (inputs TO the tool). CC numbers appear in tool results (outputs FROM the tool) — wrong interception point.',
    },
    explanation: `Without guardrail:\ntool executes → returns card_number in result → model receives it → 6% leaks to customer\n\nWith PostToolUse hook:\ntool executes → hook regex-redacts card_number → model receives clean data → cannot leak what it never saw → always safe\n\nPostToolUse fires after tool execution but before the result enters the model's context — deterministic.\n\nWhy C (PreToolUse) is the trap: it inspects tool parameters — what the agent sends TO the tool. The CC number appears in the tool result (output), not the parameters (input). PreToolUse intercepts the wrong end of the pipeline entirely.\n\nA (better system prompt) is still probabilistic — already proven to fail at 6%. D removes critical support functionality.`,
  },
  {
    id: 25,
    domain: 'D1',
    question: 'The coordinator receives two requests simultaneously: (1) rename a utility class used across 60 files, and (2) add a new health-check endpoint to one microservice. A developer proposes delegating both tasks to subagents. What is the optimal delegation strategy?',
    options: [
      'Delegate both to subagents — task 1 to a rename specialist and task 2 to an API specialist.',
      'Handle both tasks directly in the coordinator to minimise subagent communication overhead.',
      'Delegate task 1 (60-file rename) to a subagent with scoped context, and have the coordinator handle task 2 (single-file health check) directly.',
      'Delegate task 2 to a subagent and have the coordinator handle task 1, since the coordinator has full codebase awareness needed for the rename.',
    ],
    correct: 2,
    explanation: `Delegate when tasks are large or require specialist focus. Handle directly when tasks are simple and well-scoped.\n\nTask 1 (60-file rename): large scope, benefits from a dedicated subagent with focused context so it doesn't flood the coordinator.\n\nTask 2 (single-file endpoint): trivial scope, adding delegation overhead creates more complexity than the task itself warrants.\n\nOption A (delegate both) creates unnecessary overhead for a one-file change. Option B (handle both in coordinator) floods the coordinator's context with a 60-file rename — exactly what delegation is for. Option D inverts the logic: the coordinator does NOT need full codebase awareness for the rename — that is an argument FOR delegating it to a focused subagent, not against.`,
  },
  {
    id: 26,
    domain: 'D1',
    question: "A financial services company's customer support agent receives a request: 'I was charged twice for my subscription, and I also need to update my billing address.' The agent processes the address change successfully but only partially investigates the double-charge, providing an incomplete resolution. What architectural pattern should be applied to handle this correctly?",
    options: [
      'Add a system prompt instruction telling the agent to address all customer concerns before responding.',
      'Route the request to two separate specialised agents — one for billing disputes and one for address changes.',
      'Decompose the request into items, investigate each in parallel with shared context, then synthesise a unified resolution.',
      'Process the concerns sequentially — complete the address change first, then investigate the double-charge in a follow-up interaction.',
    ],
    correct: 2,
    explanation: `The request has two independent concerns that can be investigated simultaneously. Decompose → parallel investigation → unified response gives the customer one coherent resolution covering both issues.\n\nOption A (system prompt): probabilistic — the agent already dropped the double-charge investigation; more instructions don't guarantee complete handling.\n\nOption B (two separate agents): breaks the experience — customer gets two separate responses, re-identifies twice, loses the context connecting both issues (the double charge may relate to the same card being updated). Multi-concern requests need unified resolution.\n\nOption D (sequential, follow-up): forces the customer to wait for a second interaction for something resolvable now. The two concerns don't depend on each other — sequential processing wastes time with no benefit.`,
  },
  {
    id: 27,
    domain: 'D4',
    question: "A developer is iterating on a data transformation function with Claude Code and has identified three issues: (1) date parsing mishandles timezone offsets, (2) currency formatting uses the wrong locale, and (3) the output JSON schema has an incorrect field name. Issues 1 and 2 are independent; issue 3 changes the output shape both must conform to. How should the developer provide feedback?",
    options: [
      'Send all three issues in a single message to let Claude address them holistically.',
      'Send each issue in a separate sequential message, waiting for confirmation after each fix.',
      'Fix issue 3 (schema field name) first, then address issues 1 and 2 one at a time since they are independent.',
      'Use the interview pattern to have Claude ask clarifying questions about all three issues before making any changes.',
    ],
    correct: 2,
    explanation: `Issue 3 is a foundational dependency — the output schema shape determines what issues 1 and 2 must conform to. Fix 3 first, then 1 and 2 can be corrected against the right schema. This is dependency-aware sequencing.\n\nOption A (all three at once): relies on Claude detecting and respecting the dependency ordering unprompted. If Claude fixes 1 and 2 against the old schema and then renames the field, the first two fixes need to be redone.\n\nOption B (one per message): three separate round trips when 1 and 2 could be batched — more sequential than necessary.\n\nOption D (interview pattern): wrong tool. The developer already knows all three issues clearly — interview pattern is for when requirements are ambiguous and Claude needs clarification before acting.`,
  },
  {
    id: 28,
    domain: 'D1',
    question: 'A multi-agent research system has three subagents (financial filings, news, technical white papers) and a synthesis agent. Each subagent returns properly attributed findings, but the final synthesised report has no source attribution: stakeholders cannot trace which claim came from which source. Which fix addresses the root cause?',
    options: [
      'Append a bibliography section at the end of the report listing all sources each subagent consulted.',
      'Have each subagent include source URLs as inline hyperlinks in their prose output so the synthesis agent can preserve them.',
      'Require subagents to output structured claim-source mappings and instruct the synthesis agent to preserve and merge them.',
      'Store all subagent outputs in a database and have the synthesis agent reference database entries by ID instead of incorporating content directly.',
    ],
    correct: 2,
    explanation: `Root cause: the synthesis step is lossy. Even when subagents provide attributed findings, the synthesis agent rewrites them into flowing prose — dropping source information in the process. It was never instructed to carry attribution forward.\n\nOption C fixes both ends: structured mappings ensure attribution is machine-readable data (not buried in prose), and the explicit instruction to preserve and merge them fixes the synthesis step.\n\nOption A (bibliography): lists sources consulted, not which specific claim came from which source. Stakeholders still cannot trace "Q3 revenue declined 12%" to a specific filing page.\n\nOption B (inline hyperlinks): hyperlinks live inside prose sentences. When the synthesis agent rewrites — combining multiple attributed sentences into "Multiple sources indicate..." — the inline links from the original prose are dropped.\n\nOption D: removes the purpose of the synthesis agent entirely. Synthesis means reading content and producing a coherent unified narrative. Referencing DB entries by ID instead of incorporating content is assembly, not synthesis — plus it introduces unnecessary database infrastructure to solve an instruction design problem.`,
  },
];
